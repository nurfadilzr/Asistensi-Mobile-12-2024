<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Thread" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Thread" parent="Base.Theme.Thread" />

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <!-- Warna ikon pencarian -->
        <item name="android:searchIcon">@drawable/baseline_search_24</item>
        <!-- Warna teks pencarian -->
        <item name="android:textColor">@color/black</item>
        <!-- Warna latar belakang pencarian -->
<!--        <item name="android:background">@color/white</item>-->
    </style>

</resources>